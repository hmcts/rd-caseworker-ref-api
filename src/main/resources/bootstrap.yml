spring:
  cloud:
    propertiesvolume:
      prefixed: true
      paths: /mnt/secrets/s2s,/mnt/secrets/rd
      aliases:
        s2s.microservicekey-caseworker-ref-api-S2S-SECRET: S2S_SECRET
        rd.caseworker-ref-api-POSTGRES-PASS: POSTGRES_PASSWORD
        rd.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
