spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/rd
      aliases:
        caseworker-ref-api-s2s-secret: S2S_SECRET
        caseworker-ref-api-POSTGRES-PASS: POSTGRES_PASSWORD
        ApplicationInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        caseworker-topic-primary-send-listen-shared-access-key: CWRD_MQ_PASSWORD
        LD-SDK-KEY: RD_LD_SDK_KEY